[package]
name = "cand"
version = "0.1.0"
edition = "2024"
readme = "README.md"
repository = "https://github.com/CosmoBunny/cand.git"
license = "MIT"
keywords = ["embedded", "logging", "no-std", "esp32"]
categories = ["embedded", "development-tools::debugging"]

[dependencies]
ufmt = { version = "0.2", optional = true }

[features]
default = ["std", "colors"]

std = []
colors = []
ufmt = ["dep:ufmt"]

